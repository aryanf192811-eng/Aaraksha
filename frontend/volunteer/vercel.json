{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git cat-file -e $VERCEL_GIT_PREVIOUS_SHA 2>/dev/null || exit 1; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD ./",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
